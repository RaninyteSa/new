<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registracija</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-gH2yIJqKdNHPEq0n4Mqa/HGKIhSkIHeL5AyhkYV8i59U5AR6csBvApHHNl/vI1Bx" crossorigin="anonymous">
 <link rel="stylesheet" href="/resuorces/css/style.css">
 <style>
    body {
    background: rgb(11, 7, 14);
    font-family: 'Roboto', sans-serif;
    margin-top: 150px;
}

.container {
    width: 100%;
    max-width: 1140px;
    padding: 0 1rem;
}

.d-flex {
    display: flex;
    justify-content: space-between;
    align-items: center;
} 

header {
   
    padding: .5rem 0;
    box-shadow: 0 5px rgba(0, 0 , 0, 0.2);
}

header .logo img {
    max-width: 50px;
}

header nav ul {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
}

header nav ul > li > a {
    display: block;
    font-size: 16px;
    font-weight: 400;
    color: black;
    text-decoration: none;
    padding: 0 1rem;
}

header nav ul > li > a:hover {
    text-decoration: underline;
}
 </style>
</head>

<body>
  <main class="form-signin w-25 m-auto" >
  <form method="POST">
    <h1 class="h3 mb-3 fw-normal" style="  margin-top: 225px; color:antiquewhite;">Log in</h1>

    {{#if message}}
        <div class="alert alert-{{status}}">{{{message}}}</div>
    {{/if}}

    <div class="form-floating">
      <input type="text" name="idIs" class="form-control" id="floatingInput" placeholder="id kodas">
      <label for="floatingInput">id kodas</label>
    </div>
    <button class="w-100 btn btn-lg btn-outline-dark" style="background-color:black; color:antiquewhite;"type="submit">Sign in</button>
    <p class="mt-2">
        <a href="/register" style="color:white ; border: none; text-decoration: none; ">Registracija</a>
    </p>
    <p class="mt-5 mb-3 text-muted" style="text-align: center;">&copy; 2017–2022</p>
  </form>
</main>
</body>
</html>